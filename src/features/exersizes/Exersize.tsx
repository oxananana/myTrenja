import React from "react";
import styled from "styled-components";

type Props = {};

export const Exersize: React.FC<Props> = (props) => {
  return <ExersizeContainer>тыц тыц</ExersizeContainer>;
};

const ExersizeContainer = styled.div``;
